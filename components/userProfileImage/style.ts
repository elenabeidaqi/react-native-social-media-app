import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
    userProfileImageContainer: {
    borderWidth: 1,
    borderColor: '#F35BAC',
    borderRadius: 50,
    padding: 3,
  },
});
